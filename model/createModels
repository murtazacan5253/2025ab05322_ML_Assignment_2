# Import Dependencies
from dataPreProcFeatureEng import dataPreProcFeatureEng
from decisionTreeClassifier import trainDecisionTree
from knnClassifier import trainKnnClassifier
from logisticRegression import trainLogisticRegression
from naiveBayesClassifier import trainNaiveBayesClassifier
from randomForest import trainRandomForest
from xgBoost import trainXGBoost

X_train, X_test, Xs_train, Xs_test, y_train, y_test = dataPreProcFeatureEng()

trainDecisionTree(X_train, y_train)
trainKnnClassifier(Xs_train, y_train)
trainLogisticRegression(Xs_train,y_train)
trainNaiveBayesClassifier(Xs_train,y_train)
trainRandomForest(X_train, y_train)
trainXGBoost(X_train, y_train)

